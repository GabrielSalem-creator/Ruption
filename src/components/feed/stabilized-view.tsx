import { ExternalLink, Heart, Info, MessageCircleWarning, ShieldCheck, X } from "lucide-react";
import { FeedItem } from "@/lib/types";

export function StabilizedView({ app, onClose }: { app: FeedItem; onClose: () => void }) {
  return <div className="modal-overlay" role="dialog" aria-modal="true"><div className="modal-frame"><div className="iframe-shell"><iframe src={app.appUrl} sandbox="allow-scripts allow-same-origin allow-forms" loading="lazy" title={`${app.title} runtime`} /><div className="fallback-layer" style={{ pointerEvents: "none", opacity: 0 }} /></div><aside className="detail-grid"><div className="detail-grid__panel"><div className="split" style={{ marginBottom: "1rem" }}><strong>{app.title}</strong><button className="ghost-button" onClick={onClose} type="button"><X size={16} /> Exit</button></div><p className="muted">{app.hook}</p><div className="row"><span className="metric-pill">v{app.version}</span><span className="metric-pill">{app.intentLabel}</span><span className="metric-pill">{Math.round(app.recommendationScore * 100)} match</span></div></div><div className="detail-grid__panel"><div className="split" style={{ marginBottom: "0.75rem" }}><strong>Floating controls</strong><button className="ghost-button" type="button"><Info size={16} /> Details</button></div><div className="grid-2"><button className="ghost-button" type="button"><Heart size={16} /> Like</button><a className="ghost-button" href={app.appUrl} target="_blank" rel="noreferrer"><ExternalLink size={16} /> Deep link</a></div></div><div className="detail-grid__panel"><strong>Why it ranks</strong><div style={{ height: 12 }} />{app.reasons.map((reason) => <div className="chip" key={reason} style={{ marginBottom: "0.5rem" }}>{reason}</div>)}</div><div className="detail-grid__panel"><div className="row" style={{ marginBottom: "0.75rem" }}><MessageCircleWarning size={18} /><strong>Security posture</strong></div><p className="muted">Sandboxed iframe, no login-wall apps, fallback-ready for broken embeds.</p><div className="row"><ShieldCheck size={16} /><span className="chip">Sandboxed runtime</span></div></div></aside></div></div>;
}
