import { ExternalLink, Heart, Info, MessageCircle, Save, ShieldCheck, X } from "lucide-react";
import { FeedItem } from "@/lib/types";

export function StabilizedView({ app, onClose }: { app: FeedItem; onClose: () => void }) {
  return <div className="modal-overlay" role="dialog" aria-modal="true"><div className="modal-frame"><div className="iframe-shell"><iframe src={app.appUrl} sandbox="allow-scripts allow-same-origin allow-forms" loading="lazy" title={`${app.title} runtime`} /></div><aside className="detail-grid"><div className="detail-grid__panel"><div className="split"><strong>{app.title}</strong><button className="ghost-button" onClick={onClose} type="button"><X size={16} /> Exit</button></div><p className="muted">{app.hook}</p><div className="feed-status"><span className="metric-pill">v{app.version}</span><span className="metric-pill">{app.intentLabel}</span><span className="metric-pill">{Math.round(app.recommendationScore * 100)} match</span></div></div><div className="detail-grid__panel"><strong>Actions</strong><div style={{ height: 12 }} /><div className="grid-2"><button className="ghost-button" type="button"><Heart size={16} /> Like</button><button className="ghost-button" type="button"><Save size={16} /> Save</button><button className="ghost-button" type="button"><MessageCircle size={16} /> Comment</button><a className="ghost-button" href={app.appUrl} target="_blank" rel="noreferrer"><ExternalLink size={16} /> Open source</a></div></div><div className="detail-grid__panel"><div className="row"><Info size={18} /><strong>About this app</strong></div><p>{app.description}</p><div className="row">{app.tags.map((tag) => <span className="tag" key={tag}>#{tag}</span>)}</div></div><div className="detail-grid__panel"><div className="row"><ShieldCheck size={18} /><strong>Security posture</strong></div><p className="muted">Sandboxed iframe, fallback-ready rendering, and no-login-wall policy enforced.</p></div></aside></div></div>;
}
