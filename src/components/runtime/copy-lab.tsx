import { useMemo, useState } from "react";
const personas = ["Founders", "Design teams", "Growth operators"];
export function CopyLabRuntime() { const [persona, setPersona] = useState(personas[0]); const [headline, setHeadline] = useState("Ship AI launches with a live discovery feed."); const [cta, setCta] = useState("Join the first 100 creators"); const score = useMemo(() => Math.min(98, Math.round((headline.length * 0.6 + cta.length * 0.9 + persona.length * 0.7) / 2.2)), [cta.length, headline.length, persona.length]); return <div className="runtime-wrap"><div className="runtime-grid"><div className="split"><div><span className="kicker">Copy Lab</span><h1>Generate launch copy that converts</h1><p className="muted">Tune headline clarity, persona fit, and call-to-action heat in one loop.</p></div><span className="pill-status pill-status--success">CTA score {score}</span></div><div className="grid-2"><div className="runtime-card"><label className="label">Persona<select className="select" value={persona} onChange={(e) => setPersona(e.target.value)}>{personas.map((item) => <option key={item}>{item}</option>)}</select></label><label className="label">Headline<textarea className="textarea" value={headline} onChange={(e) => setHeadline(e.target.value)} /></label><label className="label">CTA<input className="input" value={cta} onChange={(e) => setCta(e.target.value)} /></label></div><div className="runtime-card"><div className="muted">Suggested output</div><h3>{headline}</h3><p>Built for <strong>{persona}</strong>. The copy emphasizes frictionless discovery, fast value, and a clear creator invitation.</p><button className="button" type="button">{cta}</button></div></div></div></div>; }
